# Node / Next.js artifacts
node_modules/
.next/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Local env — no committed secrets (DEPLOY-02; never NEXT_PUBLIC_ a credential)
.env
.env.*

# Build-time COPIED server-only assets — the repo-root sql/ aql/ data/golden/
# remain the single source of truth (D-05). The copy is regenerated by
# scripts/copy-server-assets.mjs on every prebuild/predev; never commit it
# (no duplication, no drift).
server-assets/

# OS noise
.DS_Store
